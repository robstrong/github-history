<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="robots" content="noindex,nofollow" />
    <title>{{ repo }} Issue List</title>
    <link rel="stylesheet" href="http://netdna.bootstrapcdn.com/bootstrap/3.1.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="http://netdna.bootstrapcdn.com/bootstrap/3.1.0/css/bootstrap-theme.min.css">
    <style>
        body>.container {
            background-color: #fff;
        }
        body {
            background-color: #428bca;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>{{ repo }} Issue List<small class="pull-right">Generated: {{ "now"|date("M jS, Y g:i:sa") }}</small></h1>
        <br>
        <div class="row">
            <div class="col-md-12">
                <div class="panel panel-primary">
                    <div class="panel-heading"> {{ issues|length }} Issues</div>
                    <div class="table-responsive">
                        <table class="table">
                            <tr>
                                <th>Labels</th>
                                <th>Status</th>
                                <th>Issue</th>
                                <th>Title</th>
                                <th>Detail</th>
                                <th style="min-width:150px">Creator</th>
                            </tr>
                            {% for issue in issues %}
                            <tr>
                            
                                <td>{% for labels in issue %}{% set theLabelNames = [] %}{% for labelNames in labels %}{% if labelNames.name|length > 0 %}{% set theLabelNames = theLabelNames|merge([labelNames.name]) %}{% endif %}{% endfor %}{{ theLabelNames|join(', ') }}{% endfor %}</td>
                                <td>{{ issue.state }}</td>
                                <td><a href="{{issue.html_url}}" target="_blank">#{{ issue.number }}</a></td>
                                <td>{{ issue.title }}</td>
                                <td>{{ issue.body }}</td>
                                <td><a href="{{ issue.user.html_url }}" target="_blank">{{ issue.user.login }}</a></td>
                            </tr>
                            {% endfor %}
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
